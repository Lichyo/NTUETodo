import 'package:flutter/material.dart';

class WeekColumn extends StatelessWidget {
  const WeekColumn({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(

    );
  }
}
